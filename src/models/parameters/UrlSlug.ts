import CompletionItem from '../../interfaces/completion.interface';

const URL_SLUG: CompletionItem = {
  prefix: 'url_slug',
  items: [
    'lowercase',
    'remove_stopwords',
    'separator'
  ]
};

export default URL_SLUG;