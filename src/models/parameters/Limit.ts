import CompletionItem from '../../interfaces/completion.interface';

const LIMIT: CompletionItem = {
  prefix: 'limit',
  items: [
    'characters',
    'preserve_words',
    'end_char'
  ]
};

export default LIMIT;