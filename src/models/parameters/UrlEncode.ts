import CompletionItem from '../../interfaces/completion.interface';

const URL_ENCODE: CompletionItem = {
  prefix: 'url_encode',
  items: [
    'plus_encoded_spaces',
  ]
};

export default URL_ENCODE;