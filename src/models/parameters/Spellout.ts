import CompletionItem from '../../interfaces/completion.interface';

const SPELLOUT: CompletionItem = {
  prefix: 'spellout',
  items: [
    'capitalize',
    'locale'
  ]
};

export default SPELLOUT;