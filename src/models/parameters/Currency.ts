import CompletionItem from '../../interfaces/completion.interface';

const CURRRENCY: CompletionItem = {
  prefix: 'currency',
  items: [
    'currency',
    'locale',
    'decimals'
  ]
};

export default CURRRENCY;